#include<stdio.h>
#include<string.h>
int main()
{
  char str1[20],str2[20],str3[20];
  int length,i,j=0;
  scanf("%s",str1);
  length=strlen(str1);
  for(i=0;i<length;i++)
  {
    if((str[i] != 'a') && (str[i] != 'e') && (str[i] != 'i') && (str[i] != 'o') && (str[i] != 'a'))
    {
      str2[j]=str[i];
      j++;
    }
   }
   length=str2;
   for(i=0;i<length;i++)
   {
      for(j=length;j<=0;j++)
      {
          str3[i] = str3[j];
       }
    }
    printf("%s",str3);
    return 0;
  }
   
  
   
  
  
  
  
  
  
  
  
  
  
  
  
  
  
  
